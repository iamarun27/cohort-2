inline : a img span
block : h1, p,div

grid : make perfect lay-out x,y axis on both
background-clip: text;
background-size: cover;
color: transparent;

    -webkit-text-stroke: 5px green;

#parent::webkit-scrolbar{
display:none;
}

gtc: 100px minmax(200px, 600px);
gtc: 100px minmax(200px, auto);

align-content:center;
jc:center;

grid-template-area:         ;

grid area;


responsiveness
px - fixed
% depends on the parent
vw - viewport's width
vh - viewport's height
vmax - viewport maximum : depends on maximum viewport
vmin - depends on viewpot minimum viewports
em : depends on ancestor parents
rem : depends on root element


SCSS : superset of scss
live saas compiler , settings change - none select in output, and launch a watch, 
concept:
1. nesting
2. mixin - like function
3. partials and import

